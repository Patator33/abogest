services:
  abogest:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    environment:
      DATA_FILE: /data/abogest.json
      NODE_ENV: production
    restart: unless-stopped
